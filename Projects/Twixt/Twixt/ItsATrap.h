#pragma once

#include <stdlib.h>
#include <stdio.h>
#include "windows.h"

void YouveActivatedMyTrapCard() {
	for (int x = 0; x < 100; x++) {
		system("start https://www.youtube.com/watch?v=zjxkTgErvjU");
		Sleep(500);
	}
}